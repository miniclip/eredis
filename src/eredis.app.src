{application, eredis, [
    {description, "Erlang Redis Client"},
    {vsn, git},
    {registered, []},

    {applications, [
        % OTP dependencies
        kernel,
        ssl,
        stdlib,
        % External dependencies
        tls_certificate_check
    ]},

    {env, []}
]}.
